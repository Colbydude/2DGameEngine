#pragma once

const unsigned int WINDOW_WIDTH = 800;              /** Default window width. */
const unsigned int WINDOW_HEIGHT = 600;             /** Default window height. */

const unsigned int FPS = 60;                        /** Target frames per second. */
const unsigned int FRAME_TARGET_TIME = 1000 / FPS;  /** Target frame time of 16ms. */
