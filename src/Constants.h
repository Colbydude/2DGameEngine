#pragma once

const unsigned int SCREEN_WIDTH = 256;              /** Default screen width (NES). */
const unsigned int SCREEN_HEIGHT = 240;             /** Default screen height (NES). */

const unsigned int WINDOW_WIDTH = 768;              /** Default window width. */
const unsigned int WINDOW_HEIGHT = 720;             /** Default window height. */

const unsigned int FPS = 60;                        /** Target frames per second. */
const unsigned int FRAME_TARGET_TIME = 1000 / FPS;  /** Target frame time of 16ms. */
